<nav class="navbar navbar-default">
	<%= link_to	"As of Today", home_path, class: "btn btn-link", id: "home-button" %>

	<% if logged_in?() %>

  		<%= link_to "My Portfolios", user_portfolios_path(current_user), class: "btn btn-link" %>

  		<%= link_to "New Portfolio", new_portfolio_path(current_user), class: "btn btn-link" %>

  		<%= link_to "Profile", user_path(current_user), class: "btn btn-link" %>

 		<%= link_to 'Log Out', logout_path, method: :delete, class: 'btn btn-primary pull-right' %>
	<% else %>
			<%= link_to "Create Account", new_user_path, class: 'btn btn-primary pull-right'  %>

  			<%= link_to 'Log In', login_path, class: 'btn btn-info pull-right' %>
	<% end %>
</nav>